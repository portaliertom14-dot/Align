# Edge Functions: désactiver la vérification JWT pour les endpoints publics.
# send-password-recovery-email est appelé sans utilisateur connecté (mot de passe oublié).
# Sans ce réglage, le gateway peut renvoyer 401 avec les nouveaux JWT / clés publishable.
[functions.send-password-recovery-email]
verify_jwt = false
